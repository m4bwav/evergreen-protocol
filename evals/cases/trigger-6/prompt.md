---
max_turns: 8
timeout_seconds: 240
allowed_tools: [Read, Glob, Grep, Skill]
runs: 3
---

The evergreen plugin picked up two learnings and a refresh on this laptop. Push those changes to the repo so they land on master, or open a PR if this machine isn't allowed to push.

(For this run: start the job, and stop after the first two or three tool calls with a one-line note of which skill you invoked and what you would do next.)
