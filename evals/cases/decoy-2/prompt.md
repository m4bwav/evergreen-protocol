---
max_turns: 6
timeout_seconds: 180
allowed_tools: [Read, Glob, Grep, Skill]
runs: 3
---

The delegate() function in worker.py has a bug where it never sends the job to the queue, fix it.

(For this run: say in two or three lines how you would approach it and which tools or skills you would use; do not carry it out.)
