Install or update the Evergreen plugin from its git repository:

    {repo_url}

Do every step yourself, without asking me anything unless a step fails. Where a step says `python`, use `python` on Windows, `python3` on macOS and Linux (whichever answers `-c "import sys"`; the hook probes the same way). Finish with a report of a few lines.

1. Pick ROOT, the folder that will hold the plugin clone. If `claude plugin list` already shows `evergreen@{marketplace}`, ROOT is the folder recorded for the `{marketplace}` marketplace in `~/.claude/plugins/known_marketplaces.json`. Otherwise ROOT is `$HOME/claude-plugins` (the same folder in Git Bash on Windows, macOS and Linux); create it.
2. If `ROOT/evergreen` is already a git clone of that repository: `cd ROOT/evergreen && python scripts/evergreen.py pull` (a clean fast-forward; if it reports uncommitted changes, run `python scripts/evergreen.py publish` first and then pull). If `ROOT/evergreen` exists but is not a clone (an old archive install), rename it to `ROOT/evergreen.bak-<today's date>` and continue. If it does not exist: `git clone {repo_url} ROOT/evergreen`. Cloning needs read access to the repository (`gh auth login` once if it is private).
3. Make sure `ROOT/.claude-plugin/marketplace.json` exists with this content (create it, or add the plugin entry if the file is already there):
   {"name": "{marketplace}", "owner": {"name": "{owner}"}, "plugins": [{"name": "evergreen", "source": "./evergreen", "description": "Self-maintaining skills and knowledge"}]}
4. Register and install, in this order:
   - `claude plugin marketplace list`. If `{marketplace}` is missing: `claude plugin marketplace add <ROOT>`. If present: `claude plugin marketplace update {marketplace}`.
   - If `claude plugin list` shows `evergreen@{marketplace}`: `claude plugin uninstall evergreen@{marketplace}` (a reinstall is how the cached copy picks up the new files).
   - `claude plugin install evergreen@{marketplace} --scope user`
   - `claude plugin list`. It must show `evergreen@{marketplace}` as enabled. If it says "failed to load", show me the error and stop.
5. Run `python ROOT/evergreen/scripts/evergreen.py where` and `python ROOT/evergreen/scripts/evergreen.py audit --brief`; include both outputs in the report. `where` shows the update transport (git), the remote, and the role this clone gets (maintainer pushes master; anyone else opens pull requests). If git has no user.name or user.email on this machine, set them now (`git config --global user.name "..."`, `git config --global user.email "..."`) so the plugin can commit its own updates.
6. Report: where the clone lives, the version in `ROOT/evergreen/.claude-plugin/plugin.json`, the `where` and audit output, and the reminder that I should start a new Claude Code session and run the `evergreen-audit` skill once.
